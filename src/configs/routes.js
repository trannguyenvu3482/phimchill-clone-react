const routes = {
  home: '/',
  list: '/list/:listId',
  info: '/info/:movieId',
  watch: '/xem/:movieId',
  search: '/tim-kiem/:query',
  login: '/login',
  register: '/register',
};

export default routes;
